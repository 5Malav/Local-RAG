# Embeddings

This folder stores vector embeddings generated from document chunks.

## Files
- `embed.py`: Script to generate embeddings
- `*.pkl`: Stored embeddings (ignored in Git)

## Notes
Embeddings are generated using SentenceTransformers and are used for vector search in FAISS.